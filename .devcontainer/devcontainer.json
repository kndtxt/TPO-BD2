{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
  },
  "postCreateCommand": "sudo apt update && sudo apt install -y python3.8 python3.8-pip && pip3 install -r requirements.txt && sh run.sh"
}
